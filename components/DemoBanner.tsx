export default function DemoBanner() {
  return (
    <div className="bg-ink text-paper">
      <div className="mx-auto max-w-7xl px-6 py-2 flex items-center justify-between text-[11px] uppercase tracking-[0.18em] font-mono">
        <span className="opacity-80">
          Demo — Reflow Automations × Digital Concepts Factory
        </span>
        <span className="hidden sm:inline opacity-60">
          Niet alle pagina&apos;s zijn uitgewerkt voor deze showcase
        </span>
      </div>
    </div>
  );
}
